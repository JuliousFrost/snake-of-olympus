export const CONTROLS = {
  left: ['A', 'ArrowLeft'],
  right: ['D', 'ArrowRight'],
  boost: ['W', 'ArrowUp'],
  fire: ['SPACE'],
  charge: ['F'],
  pause: ['ESC', 'P'],
  restart: ['R'],
} as const;
